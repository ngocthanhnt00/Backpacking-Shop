const handleRender = () => {
   for (let  = 0;  < array.length; ++) {
    const element = array[];
    
   }
}

let dataLocal = JSON.parse(localStorage.getItem('product'));
if(dataLocal.length > 0) {
    handleRender()
}


